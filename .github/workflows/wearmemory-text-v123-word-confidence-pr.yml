name: WearMemory Text 1.2.3 Word Confidence

on:
  pull_request:
    branches: [ wearmemory-text-whisper-production ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 45
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build WearMemory Text 1.2.3
        shell: bash
        run: bash wearmemory_text_v121/build_production_v123.sh

      - name: Upload verified 1.2.3 IPA
        uses: actions/upload-artifact@v4
        with:
          name: WearMemoryText-v1.2.3-DEElektro-WordConfidence-CPU-NoBLAS-iOS15
          path: |
            /tmp/WearMemoryText_v1.2.3b23_DEElektro_WordConfidence_CPU_NoBLAS_iOS15.ipa
            /tmp/WearMemoryText_v1.2.3b23_DEElektro_WordConfidence_CPU_NoBLAS_iOS15.ipa.sha256
            /tmp/WearMemoryText_v1.2.3_Source.zip
            /tmp/wmtext121-entitlements.plist
            /tmp/wmtext121-build.log
          compression-level: 0
          retention-days: 7
